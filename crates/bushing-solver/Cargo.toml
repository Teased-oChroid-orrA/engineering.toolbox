[package]
name    = "bushing-solver"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust structural bushing solver — offline, zero-admin"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror  = "2"

[dev-dependencies]
proptest = "1"
