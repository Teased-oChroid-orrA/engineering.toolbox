// Hash-router mode (kit.router.type = 'hash')
//
// In recent SvelteKit versions, exporting page options such as `ssr` or
// `prerender` is ignored under the hash router and can trigger a runtime error.
// Keep this file present (reserved for future shared load hooks), but do not
// export those options.
